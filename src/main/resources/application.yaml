#端口号
server:
  port: 8081
spring:
  application:
  name: "mqtt-server"
  mqtt:
    username: "admin"
    password: "admin123"
    url: "tcp://127.0.0.1:1883" #可配置多个
    topic: "iot/mqtt/the/first"
    completionTimeout: "3000"
    client:
      id-consumer: "mqttId-consumer"#订阅和发布不能使用相同的id
      id-product: "mqttId-product"